# RDS

Can have many read replicas. one can direct read tasks e.g. data analysis to read replicas. This leaves master free and performant for your main application.

multi-az deployment - master and standby are always synced.

easy to vertically scale your database

host replacement??

lower admin burden

- infrastruture provisioning


storage options
- gp2 - busrtable upto 3000 IOPS
- io1 - consistent upto 40000 IOPS per DB instance -OLTP database workloads
magnetic/standard storgae is also supported for backward compatibility.

in case of infrastructure failure, aws switches to standby instance in same region or another region.
application can resume as soon as failover is complete
db instance's dns name record remains unchanged during the failover providing automated recovery

Recurive name server
root servers (13)
TLD - top level domain nameserver
Authoritative Nameserver


aurora multi-master to scale write operations

read replicas scale read operations for traditional dbs


