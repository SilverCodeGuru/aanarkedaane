# EC2

## Systems Manager

operations hub

session manager is a capability of systems manager which offer secure node management.


EC2 instance connect

removes SSH key management requirement

IP v6 addresses are public and reachable over internet

IP v4 addresses are private

most architectures have few addresses for public subnet and many more for private subnet

default nacl allows all traffic

custom nacl disallows everything

priority is detemined by number of the rule. lower number has higher priority

NAT gateway to allow private subnets to access interent
NAT gateway resides in public subnet
NAT gateway only support IP v4 addresses
resources from internet can not initiate connections to resources in private subnet
NAT gateway IP address must be elastic

Elastic IP address is a static public IP v4 address 

can move rapidly from one instance to another or from one network interface to another

Egress only internet gateway for IPv6 addresses 


VPC Peering to connect VPCs

peering traffic remains on amazon backbone
