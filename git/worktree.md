# worktree

easy way to get a new branch in another folder for review/testing/merging without polluting your work directory

```
git worktree add ../int-test feature/theirs
git worktree remove ../int-test
git worktree prune
``