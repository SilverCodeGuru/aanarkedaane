# web stuff

- python -m http.server serves all the files from current folder?

curl -I: response headers

curl -o - save

curl -v: verbose - all headers and contents at the top

wget

saves the whole thing

127.0.0.1 - loopback IP address for localhost hostname