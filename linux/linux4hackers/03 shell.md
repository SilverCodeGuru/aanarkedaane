# shell

- PS1 variable defines how the prompt looks like on a shell
- when nothing is defined, simple $/# might show as prompt
- it can be customized and might be helpful during penetration testing

